<Window x:Class="LifeManager.Views.TransactionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LifeManager.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        
        Title="TransactionsView" Height="500" Width="800" Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid Background="#0F172A">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>  
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        
        <Border 
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Grid.Row="0"
            Grid.RowSpan="4"
            Background="#1E293B"
            CornerRadius="12"
            Padding="20"
            Margin="20"
      >

            <StackPanel>

                <!-- 日付の入力欄。選択値は ViewModel の SelectDate へバインドする。 -->
                <TextBlock Text="日付"
                   Foreground="White"
                   FontSize="20"
                   Margin="0,0,0,8"/>

                <DatePicker
            SelectedDate="{Binding SelectedDate}"
            Style="{StaticResource MaterialDesignOutlinedDatePicker}"
            materialDesign:HintAssist.Hint="日付を選択"
                   Foreground="White"
            Margin="0,0,0,20"/>



                <TextBlock Text="収入"
                            FontSize="20"
                            Foreground="White"/>

                <!--
                    なぜ UpdateSourceTrigger=PropertyChanged か:
                        既定（フォーカス喪失時）ではなく 1 文字ごとに ViewModel へ反映させ、
                        導出プロパティの合計をリアルタイムに更新させるため。
                    なぜ ValidatesOnDataErrors=True か:
                        検証を ViewModel の IDataErrorInfo に問い合わせ、エラー時に枠を赤くするため。
                        検証ルール自体は ViewModel 側に置き、View には判定を持たせない。
                -->
                <TextBox
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    Text="{Binding Income, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0, ValidatesOnDataErrors=True}"
                    PreviewTextInput="NumberOnly_PreviewTextInput"   
                    DataObject.Pasting="NumberOnly_Pasting"
                    Height="50"
                    Margin="0,5,0,15"
                    materialDesign:HintAssist.Hint="収入"
                    BorderBrush="White"
                    BorderThickness="1.5"
                    Foreground="White"/>

                <TextBlock Text="支出"
                            FontSize="20"
                            Foreground="White"/>

                <TextBox 
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    Text="{Binding Expense, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0, ValidatesOnDataErrors=True}"
                    PreviewTextInput="NumberOnly_PreviewTextInput"   
                    DataObject.Pasting="NumberOnly_Pasting"
                    Height="50"
                    Margin="0,5,0,15"
                    materialDesign:HintAssist.Hint="支出"
                    BorderBrush="White"
                    BorderThickness="1.5"
                    Foreground="White"/>

            </StackPanel>
        </Border>
        
        <!-- 表示ボタン。押下を ViewModel の LoadCommand へ委ね、一覧取得を実行させる。 -->
            <Button Content="表示"
                    Margin="0,10,0,10"
                    Command="{Binding LoadCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Grid.Column="2"
                    Grid.Row="3"
                    Grid.ColumnSpan="2"/>

            <!-- 取引一覧。ViewModel の Transactions にバインドし、状態変化に追従して表示する。 -->
        <DataGrid ItemsSource="{Binding Transactions}"
                      AutoGenerateColumns="False"
                      Background="LightBlue"
                      Height="300"
                      Grid.Column="2"
                      Grid.Row="0"
                      Grid.ColumnSpan="2"
                      Grid.RowSpan="3"/>    

        <!-- 合計表示エリア。値は ViewModel の導出プロパティ Total にバインドする。 -->
        <Border
            Grid.Column="0"
            Grid.Row="4"
            Grid.ColumnSpan="2"
            BorderBrush="White"
            BorderThickness="1.5"      
            Background="#1E293B"
            CornerRadius="12"
            Padding="10"
            Margin="20"
            >

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
            

               <!-- 合計の見出し（固定文言。金額本体は右隣の Total バインドで表示）。 -->
        <TextBlock Text="合計"
                   Foreground="White"
                   FontSize="18"
                   VerticalAlignment="Center"/>


                <TextBlock Text="{Binding Total, StringFormat='{}{0:N0}円'}"
           Foreground="White"
           FontSize="20"
           Margin="0,10,0,0" 
           Grid.Column="1"    
           VerticalAlignment="Center"
           />
            </Grid>
        </Border>

        <Button Grid.Column="3"
                Grid.Row="4"
                Content="保存"
                Style="{StaticResource MaterialDesignRaisedButton}"
                Command="{Binding SaveCommand}">    
        </Button>
    </Grid>
</Window>
